/**
 * Step 7: Strict Mode & Default Binding
 * 
 * Task:
 * 1. Create a normal function logging 'this'. Call it standalone (default binding).
 * 2. Enable strict mode using "use strict"; at the top of the file.
 * 3. Call the function again and explain why 'this' becomes undefined instead of global/window.
 */

// Write your code here

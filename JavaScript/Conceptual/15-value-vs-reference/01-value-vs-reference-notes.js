/**
 * Conceptual Q2: Value vs Reference Types
 * Folder: 15-value-vs-reference
 *
 * Tasks:
 * 1. List primitives (by value) vs non-primitives (by reference).
 * 2. Mentally predict:
 *      let a = 10; let b = a; b = 20;        → a ?
 *      let x = {n:1}; let y = x; y.n = 2;   → x.n ?
 * 3. Explain WHY obj2 = obj1 both change (heap reference).
 * 4. Ways to break reference (shallow ideas only): spread, Object.assign.
 * 5. Tie to Q1: object data heap pe; variable stack pe reference hold karti hai.
 *
 * Write your notes / tiny demo below when ready...
 */

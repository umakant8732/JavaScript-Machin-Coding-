/**
 * Conceptual Q3: == vs === and Truthy / Falsy
 * Folder: 16-equality-truthy-falsy
 *
 * Tasks:
 * 1. Difference: == (loose / coercion) vs === (strict). Prefer kaunsa aur kyun?
 * 2. Falsy list yaad karo: 0, "", null, undefined, NaN, false
 * 3. Predict without running:
 *      0 == false
 *      0 === false
 *      "" == false
 *      null == undefined
 *      null === undefined
 * 4. ?? vs || — kab kaunsa? (nullish vs any falsy)
 * 5. Real app example: if (userName) vs if (userName ?? "Guest")
 *
 * Write your notes / tiny demo below when ready...
 */

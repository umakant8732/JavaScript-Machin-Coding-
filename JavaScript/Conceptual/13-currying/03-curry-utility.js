/**
 * Step 3: Generic curry() Wrapper Utility
 * 
 * Task:
 * 1. Write a helper function 'curry(fn)' that takes a normal function and returns its curried version.
 * 2. Example: curry(multiply)(2)(3)(4) dynamically runs when total parameter length matches original arguments.
 */

// Write your code here

/**
 * Conceptual Q16: DOM Select / Create Nodes
 * Folder: 23-dom-select-create
 *
 * Tasks:
 * 1. querySelector vs querySelectorAll vs getElementById — when to use which?
 * 2. createElement + appendChild flow (write the steps)
 * 3. textContent vs innerHTML — when is there an XSS risk?
 * 4. classList: add / remove / toggle — use cases
 * 5. Mini plan: "Add a <li> to a list with text and a class" — steps in comments
 *
 * Write your notes / tiny demo below when ready...
 * (HTML file can be created later — comments are enough for now)
 */

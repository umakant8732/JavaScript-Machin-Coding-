/**
 * Conceptual Q13: Promises + Promise.all vs Promise.allSettled
 * Folder: 20-promises
 *
 * Tasks:
 * 1. States: pending → fulfilled / rejected — explain
 * 2. .then / .catch / .finally — role of each
 * 3. Why Promise callbacks are microtasks (link later to event-loop folder)
 * 4. Promise.all — one-liner: fail-fast (one rejection → overall rejection)
 * 5. Promise.allSettled — one-liner: wait for all; returns each result along with its status
 * 6. Scenario: 3 APIs; if 1 fails, the remaining 2 should still be displayed in the UI → use Promise.all or Promise.allSettled?
 *
 * Write your notes / tiny demo below when ready...
 */

/**
 * Conceptual Q1: Call Stack vs Heap
 * Folder: 14-call-stack-vs-heap (existing 01–13 touch mat kiye)
 *
 * Tasks (comments only — pehle samajho, phir khud examples likhna):
 * 1. Apne words mein likho: Call Stack kya hai? Heap kya hai?
 * 2. Neeche diagram comment mein complete karo (fill the blanks):
 *
 *    CALL STACK (execution)          HEAP (data)
 *    ┌─────────────────┐             ┌──────────────────────┐
 *    │ ???             │             │  objects / arrays    │
 *    │ ???             │  reference  │  live here           │
 *    │ ???             │ ──────────► │                      │
 *    └─────────────────┘             └──────────────────────┘
 *
 * 3. Explain: primitive value stack pe copy kyun hoti hai / object sirf reference kyun?
 * 4. Predict: function A calls B calls C — stack pe order kya hoga (push/pop)?
 * 5. Interview one-liner yaad karo: Stack = "kaunsa fn chal raha"; Heap = "data kahan pada"
 *
 * Write your notes / tiny demo below when ready...
 */

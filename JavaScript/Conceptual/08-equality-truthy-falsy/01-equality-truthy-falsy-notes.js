/**
 * Conceptual Q3: == vs === and Truthy / Falsy
 * Folder: 16-equality-truthy-falsy
 *
 * Tasks:
 * 1. Difference: == (loose / coercion) vs === (strict). Which one to prefer and why?
 * 2. Memorize the falsy list: 0, "", null, undefined, NaN, false
 * 3. Predict without running:
 *      0 == false
 *      0 === false
 *      "" == false
 *      null == undefined
 *      null === undefined
 * 4. ?? vs || — when to use which? (nullish vs any falsy)
 * 5. Real app example: if (userName) vs if (userName ?? "Guest")
 *
 * Write your notes / tiny demo below when ready...
 */

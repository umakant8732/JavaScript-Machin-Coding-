/**
 * Problem 13: Currying
 * 
 * Implement currying mechanisms.
 * Currying splits a function that takes multiple arguments into a sequence of nested functions 
 * that each take a single argument.
 */

// 1. TODO: Implement a function that behaves as add(1)(2)(3)... until evaluated
// E.g. add(1)(2)(3)() -> 6
function curriedAdd(a) {
    // Write curry logic here
}

/*
console.log(curriedAdd(1)(2)(3)()); // Expected: 6
console.log(curriedAdd(5)(10)());    // Expected: 15
*/


// 2. TODO: Implement a general currying converter function curry(fn)
function curry(fn) {
    // Converts sum(a,b,c) to curried sum(a)(b)(c)
}
